Aquí tienes la traducción a **inglés americano**:

---

## 📝 Pending Task Manager (To-Do List) in Python

This repository contains a simple Python script designed to manage a to-do list through a console interface.

---

## 🛠️ Technical Requirements

The purpose of this program is to allow users to manage a list of pending tasks. The program should allow:

* **Add Task**: This option lets the user enter a new task and adds it to the to-do list.
* **List Tasks**: Displays a numbered list of all pending tasks, indicating whether they are completed or not.
* **Delete Task**: Allows the user to select a task from the list and remove it.
* **Exit**: Ends the program.

## 🚀 Usage

To run the program, simply execute the Python script in your terminal:

#### Windows

```bash
python main.py
```

#### Linux / macOS:

```bash
python3 main.py
```
